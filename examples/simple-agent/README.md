# Simple Agent Example

Run the root demo:

```bash
pnpm dev
```

The demo creates an `Engine` with `MockProvider`, `InMemoryStore`, and an empty `DefaultToolRegistry`, then returns a mock assistant message.
